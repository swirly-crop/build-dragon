# Kotlin Auditor

A Kotlin implementation for Android auditing (JVM-based).

## Building

Requires Kotlin compiler (`kotlinc`).

Run `make` to build the JAR, then `make run` to execute.

Note: For Android, use Android Studio with Kotlin.