# Assembly Example

Tiny x86_64 Linux assembly example showing a safe, non-destructive program that prints a short message and exits. For educational purposes only.

Build and run:

```bash
gcc -nostdlib -static -o android-auditor/assembly/hello android-auditor/assembly/hello.s
./android-auditor/assembly/hello
```
