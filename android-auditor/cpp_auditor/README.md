# C++ Android Auditor

Small C++ CLI that invokes `adb` to collect basic device diagnostics. Intended for device owners and defenders only.

Build and run:

```bash
make -C android-auditor/cpp_auditor
./android-auditor/cpp_auditor/cpp_auditor --devices
```
