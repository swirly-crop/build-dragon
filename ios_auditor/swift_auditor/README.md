# Swift Android Auditor

Small Swift CLI for macOS/Linux that invokes `adb` to collect device diagnostics.

Build & run (macOS):

```bash
swiftc android-auditor/swift_auditor/main.swift -o android-auditor/swift_auditor/swift_auditor
./android-auditor/swift_auditor/swift_auditor --devices
```
